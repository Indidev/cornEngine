tag: arm //unique tag
img: arm.png //image of spirit (relative to the configuration)
col: arm_col.png //collision model (either .col as primitives or an image)
root: 17.121 //point where the spirit is placed with
pivot: 17.121	//optional rotation point, center is default
//if root is not given, point to place is 0 and rotation point is it's center
//fSize: 34.134 //optional frame size (needed if picture > frame and animated spirits
//fPos: 0.0 //optional frame position (e.g. multiple spirits in one picture)
//fps: 1 //optional frames per second for animations
//fTime: 1000 //optional frame time in ms (fps * 1000) //fTime is prefered before fps if both is given

